# 项目意义
这个项目是帮助自己不断提升感知时间、珍惜时间能力的助力工具。像是每天最后都要进行一场大考，监督自己不那么肆意浪费时间。
# 关键步骤
- [项目意义](#项目意义)
- [关键步骤](#关键步骤)
  - [零：配置环境](#零配置环境)
    - [1. 安装所需的依赖包](#1-安装所需的依赖包)
    - [2. 填写好 .env 文件里的信息](#2-填写好-env-文件里的信息)
  - [一、Python读取Notion表格信息](#一python读取notion表格信息)
    - [1. 在notion里创建一个database](#1-在notion里创建一个database)
    - [2. 代码验证](#2-代码验证)
  - [二、Python调用Ollama里的模型读取文本里的时间信息](#二python调用ollama里的模型读取文本里的时间信息)
    - [1. 下载安装配置oLLama，下载开源模型Qwen3：4b模型,并验证模型是否能够成功运行](#1-下载安装配置ollama下载开源模型qwen34b模型并验证模型是否能够成功运行)
    - [2. 代码验证](#2-代码验证-1)
  - [三、Python发飞书](#三python发飞书)
    - [1. 添加自定义机器人](#1-添加自定义机器人)
    - [2. 代码验证](#2-代码验证-2)
- [注意事项](#注意事项)
    - [1. 不同项目的依赖项通过创建venv虚拟环境独立开](#1-不同项目的依赖项通过创建venv虚拟环境独立开)
    - [2. 每次要跑代码的时候，记得用以下代码先进入vev环境](#2-每次要跑代码的时候记得用以下代码先进入vev环境)
    - [3. 版本管理](#3-版本管理)
- [项目可更新部分](#项目可更新部分)

## 零：配置环境
### 1. 安装所需的依赖包
- 在项目文件夹下打开PowerShell
- 查看一下Python是否安装，版本是否符合要求 `python --version`
- 先`python -m venv .venv`后`.\.venv\Scripts\activate`，创建虚拟环境并激活(好处就是可以把不同项目的依赖项环境隔离开，防止不同项目之间的依赖项版本之间发生冲突。)
- 安装所有项目依赖 `pip install -r requirements.txt`
- `pip list` 查看该有的依赖是否都安装成功

### 2. 填写好 .env 文件里的信息
- 要从 Notion 自建的数据表里提取数据，需要以下信息：Notion Token，数据表对应的 ID（Database ID），用于定位对应内容的变量（例如本项目中是 date，通过 date 拿到对应的 text）。
- 要通过 Ollama 调用大模型，就需要本地的服务端口号。一般 Ollama 在安装的时候，都默认了一个本地运行的服务端口，也就是`http://localhost:11434`
- 要将外部信息发到飞书里的群聊中，就需要通过 Webhook 这个端口链接。

## 一、Python读取Notion表格信息
### 1. 在notion里创建一个database
- 新建一个页面，名字就叫raw time logs
- 在页面里输入/database
### 2. 代码验证
- `python notion_read.py 2026-07-02` 终端如果能打印出notion那一天的对应text，就证明成功了，跑通了


## 二、Python调用Ollama里的模型读取文本里的时间信息
### 1. 下载安装配置oLLama，下载开源模型Qwen3：4b模型,并验证模型是否能够成功运行
- 任何一个目录下打开powershell输入`ollama pull qwen3:4b` 下载模型
- `ollama run qwen3:4b` 和大模型对话，看能不能得到回复，能的话证明大模型能成功运行
### 2. 代码验证
- `python ollama_extract.py 2026-07-02` 终端如果能够打印出JSON形式的信息，说明这一步跑通了


## 三、Python发飞书
### 1. 添加自定义机器人
- 在飞书里自建一个群聊，然后添加自定义机器人，生成机器人后，把飞书软件的webhook链接号放到.env环境代码里面来（Webhook就相当于是一个服务的收件箱地址）
### 2. 代码验证
- `python feishu_report.py 2026-07-02`


# 注意事项
### 1. 不同项目的依赖项通过创建venv虚拟环境独立开
- `python -m venv .venv`
- `.\.venv\Scripts\activate`
- `pip install -r requirements.txt`
### 2. 每次要跑代码的时候，记得用以下代码先进入vev环境
- `.\.venv\Scripts\Activate.ps1` 成功进入 venv 环境之后，终端会显示 venv 的标识。由于项目之间的依赖项是隔开的，分别打包在各自的 venv 环境里，所以如果要复现项目的执行结果，就需要进入对应项目的 venv 环境里，再去跑执行代码才可以。
### 3. 版本管理
- 把能跑通的 MVP 先上传到 GitHub，这样保证有一个最小可行性的版本，后面再做新功能添加的时候就更容易做版本控制。
- 上传到 GitHub 的项目中，可以除去以下内容，保持代码的简洁：
1. venv 虚拟环境文件。
2. .env文件，里面包含个人的隐私密钥。
3. 缓存临时文件：如 __pycache__。
4. 本地测试产物：如 .log、.png 等文件。

# 项目可更新部分
- 最终报告除了发送以外，也要写一份，整理到 Notion 的一个数据库里。
- 通过微信或者邮件进行报告发送，因为飞书还是不经常使用
- 比较当天和昨天的情况，以及当天和上周的平均情况
- 每个星期日都可以总结一次这个星期的平均有效利用时间，并和上个星期的平均有效利用时间进行对比
- 每天定时自动化运行一次
- 将每天的时间利用状况表现良好与否，记录在 Tim Urban 的星期日历上，并在每个格子上色，这样可以激励自己珍惜时间。
- 调用AI分析每一天有效利用时间集中在什么时间区域以及浪费时间的重灾区是在什么时间段
- 输入端使用更智能的一键式整合输入策略，比如最理想的就是，对着手机说一句话，活动就自动记录在了notion对应的表格里了，不需要人工打开notion去记录。